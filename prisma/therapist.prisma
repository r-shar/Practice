model Therapist {
  id        String   @id @default(cuid())
  clerkId   String   @unique

  firstName String?
  lastName String?
  email String?

  createdAt DateTime @default(now())

  clients   Client[]
}